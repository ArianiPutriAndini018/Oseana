import 'package:flutter/material.dart';

class FloatingVideoCard extends StatelessWidget {
  final Widget child;

  const FloatingVideoCard({
    super.key,
    required this.child,
  });

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
